import React from "react";
import { createRoot } from "react-dom/client";
import Loom from "../loom-ui.jsx";

createRoot(document.getElementById("root")).render(<React.StrictMode><Loom /></React.StrictMode>);
