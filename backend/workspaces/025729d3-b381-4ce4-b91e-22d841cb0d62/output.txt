{
  "explanation": "Added a suite of pytest unit tests for the SQLAlchemy models and FastAPI task endpoints, integration tests that exercise the full task lifecycle, React Testing Library UI tests for the task component, and a GitHub Actions CI workflow to run them automatically.",
  "files": [
    {
      "path": "backend/tests/test_models.py",
      "code": "import pytest\nfrom sqlalchemy import create_engine\nfrom sqlalchemy.orm import sessionmaker\nfrom backend.app import Base, Task, SubTask\n\n# Use an in‑memory SQLite database for fast isolated tests\nENGINE = create_engine('sqlite:///:memory:')\nTestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=ENGINE)\n\n@pytest.fixture(scope=\"function\")\ndef db_session():\n    Base.metadata.create_all(bind=ENGINE)\n    session = TestingSessionLocal()\n    yield session\n    session.close()\n    Base.metadata.drop_all(bind=ENGINE)\n\ndef test_task_requires_title(db_session):\n    # title column is non‑nullable\n    task = Task(description=\"Sample task without title\")\n    db_session.add(task)\n    with pytest.raises(Exception):\n        db_session.commit()\n    db_session.rollback()\n\ndef test_subtask_relationship_and_constraints(db_session):\n    # Create a parent task\n    parent = Task(title=\"Parent\", description=\"Parent task\")\n    db_session.add(parent)\n    db_session.commit()\n\n    # SubTask must reference a valid Task id and have a non‑negative status\n    sub = SubTask(task_id=parent.id, payload={\"data\": 1}, status=-1)\n    db_session.add(sub)\n    with pytest.raises(Exception):\n        db_session.commit()\n    db_session.rollback()\n\n    # Valid subtask\n    sub = SubTask(task_id=parent.id, payload={\"data\": 1}, status=0)\n    db_session.add(sub)\n    db_session.commit()\n    assert sub.id is not None\n    assert sub.task_id == parent.id"
    },
    {
      "path": "backend/tests/test_task_api.py",
      "code": "import json\nimport pytest\nfrom fastapi.testclient import TestClient\nfrom backend.main import app\n\nclient = TestClient(app)\n\n@pytest.fixture\ndef create_task():\n    def _create(payload):\n        response = client.post(\"/tasks\", json=payload)\n        assert response.status_code == 201\n        return response.json()\n    return _create\n\ndef test_create_task_success(create_task):\n    payload = {\n        \"title\": \"Process Data\",\n        \"description\": \"Split data into chunks\",\n        \"data\": [1, 2, 3, 4, 5, 6]\n    }\n    task = create_task(payload)\n    assert task[\"title\"] == payload[\"title\"]\n    assert len(task[\"subtasks\"]) == 3  # assuming split into 3 chunks\n\ndef test_create_task_missing_title():\n    payload = {\"description\": \"No title\", \"data\": []}\n    response = client.post(\"/tasks\", json=payload)\n    assert response.status_code == 422  # validation error from pydantic\n\ndef test_assign_subtask_and_aggregate_results(create_task):\n    # Create a task first\n    task = create_task({\"title\": \"Aggregate\", \"description\": \"\", \"data\": [10, 20, 30]})\n    sub_id = task[\"subtasks\"][0][\"id\"]\n\n    # Simulate a worker assigning itself to a subtask\n    assign_resp = client.post(f\"/subtasks/{sub_id}/assign\", json={\"worker_id\": \"worker-1\"})\n    assert assign_resp.status_code == 200\n    assert assign_resp.json()[\"worker_id\"] == \"worker-1\"\n\n    # Submit a result for the subtask\n    result_payload = {\"result\": 42}\n    result_resp = client.post(f\"/subtasks/{sub_id}/result\", json=result_payload)\n    assert result_resp.status_code == 200\n    assert result_resp.json()[\"status\"] == \"completed\"\n\n    # After all subtasks are completed, the parent task should be aggregated\n    # For simplicity, we manually complete remaining subtasks\n    for sub in task[\"subtasks\"][1:]:\n        client.post(f\"/subtasks/{sub[\"id\"]}/assign\", json={\"worker_id\": \"worker-1\"})\n        client.post(f\"/subtasks/{sub[\"id\"]}/result\", json={\"result\": 0})\n\n    agg_resp = client.get(f\"/tasks/{task[\"id\"]}/aggregate\")\n    assert agg_resp.status_code == 200\n    agg_data = agg_resp.json()\n    assert agg_data[\"aggregated_result\"] == 42  # depends on aggregation logic"
    },
    {
      "path": "backend/tests/test_integration_task_flow.py",
      "code": "import json\nimport pytest\nfrom fastapi.testclient import TestClient\nfrom backend.main import app\n\nclient = TestClient(app)\n\ndef test_full_task_lifecycle():\n    # 1. Create a new task that will be split into 2 subtasks\n    create_payload = {\n        \"title\": \"Sum Numbers\",\n        \"description\": \"Sum a list of numbers using workers\",\n        \"data\": [1, 2, 3, 4]\n    }\n    create_resp = client.post(\"/tasks\", json=create_payload)\n    assert create_resp.status_code == 201\n    task = create_resp.json()\n    assert len(task[\"subtasks\"]) == 2\n\n    # 2. Simulate two workers picking up subtasks\n    for sub in task[\"subtasks\"]:\n        assign = client.post(f\"/subtasks/{sub[\"id\"]}/assign\", json={\"worker_id\": f\"worker-{sub[\"id\"]}\"})\n        assert assign.status_code == 200\n\n    # 3. Workers submit partial sums\n    partial_results = [3, 7]  # 1+2 = 3, 3+4 = 7\n    for sub, result in zip(task[\"subtasks\"], partial_results):\n        resp = client.post(f\"/subtasks/{sub[\"id\"]}/result\", json={\"result\": result})\n        assert resp.status_code == 200\n        assert resp.json()[\"status\"] == \"completed\"\n\n    # 4. Verify aggregation endpoint returns the total sum\n    agg_resp = client.get(f\"/tasks/{task[\"id\"]}/aggregate\")\n    assert agg_resp.status_code == 200\n    agg = agg_resp.json()\n    assert agg[\"aggregated_result\"] == sum(partial_results)  # expects 10\n\n    # 5. Ensure task status is marked as completed\n    status_resp = client.get(f\"/tasks/{task[\"id\"]}\")\n    assert status_resp.status_code == 200\n    assert status_resp.json()[\"status\"] == \"completed\""
    },
    {
      "path